import { Monitor } from '@indoor-nav/shared';
interface Props {
    monitors: Monitor[];
    onClose: () => void;
}
export default function MonitorPanel({ monitors, onClose }: Props): import("react/jsx-runtime").JSX.Element;
export {};
